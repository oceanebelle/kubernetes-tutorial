apiVersion: v1
kind: Service
metadata:
  name: {{ include "myjob2.fullname" . }}
  labels:
    {{- include "myjob2.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "myjob2.selectorLabels" . | nindent 4 }}
